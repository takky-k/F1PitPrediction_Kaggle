# Notebooks

This document is written in both English and Japanese. English appears first, followed by Japanese translation.

## English

Use this folder for the retrospective notebook version of the work.

Suggested notebook:

```text
f1_pit_prediction_retrospective.ipynb
```

The current cleaned repository keeps the scripts and compact report first, so the notebook can be built from stable artifacts instead of from large generated prediction files.

## 日本語

この文書は英語と日本語の両方で書いています。最初に英語、そのあとに日本語訳を置いています。

このフォルダは、振り返り用 notebook を置く場所です。

想定 notebook:

```text
f1_pit_prediction_retrospective.ipynb
```

現在の整理済みリポジトリでは、まずスクリプトと一体型レポートを残しています。そのため、後から大きな予測ファイルではなく安定した要約成果物をもとに notebook を作れます。
